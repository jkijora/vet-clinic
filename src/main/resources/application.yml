server:
  port : 8082
spring:
  jpa:
    hibernate:
      ddl-auto: update
  h2.console:
    enabled: true
    path: '/console'
  datasource:
    url: 'jdbc:h2:mem:testdb'
    driverClassName: org.h2.Driver
    username: sa
    password:
